package graph;

/**
 * Interface that defines a class that can be used to deal with command line prompts.
 * @author i2scmg
 *
 */
public interface Command {

    public String execute(String args);
    
}
